# Optional: Set up automatic sync with GitHub

# Method 1: Using GitHub CLI for easy repo management
gh repo view  # See repo info
gh issue list # See issues
gh pr list    # See pull requests

# Method 2: Set up pre-commit hooks (optional)
# Create .git/hooks/pre-commit file to run tests before commits

# Method 3: Set up GitHub Actions for deployment
# Create .github/workflows/deploy.yml for automatic deployment

# Method 4: Use Windsurf's built-in features
# - Right-click files > Git > various options
# - Use integrated terminal for Git commands
# - View Git history in timeline view
